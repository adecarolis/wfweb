#include "rigslot.h"

// Out-of-line anchor so the vtable, typeinfo, and moc-generated members are
// emitted in one translation unit instead of every TU that includes the
// header. Pure interface otherwise — no member body to provide.
