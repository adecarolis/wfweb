/*
 *  dedupe.h  (wfweb stub)
 *
 *  Upstream dlq.c includes dedupe.h but does not call any of its functions
 *  (dedupe is the digipeater duplicate-suppression cache).  We provide an
 *  empty stub header so the include resolves without pulling in the full
 *  digipeater code.
 */

#ifndef DEDUPE_H
#define DEDUPE_H 1

#endif /* DEDUPE_H */
